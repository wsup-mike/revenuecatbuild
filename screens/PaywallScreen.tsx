import { View, Text, SafeAreaView } from 'react-native'

const PaywallScreen = () => {
  return (
    <SafeAreaView>
      <Text>PaywallScreen</Text>
    </SafeAreaView>
  )
}

export default PaywallScreen